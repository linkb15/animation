import { ReactSpring } from "@/app/react-spring";

export default function Home() {
	return (
		<main className="flex min-h-screen overflow-hidden justify-center items-center">
			<ReactSpring />
		</main>
	);
}
